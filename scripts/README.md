# Scripts
Deterministic helpers for ingestion and maintenance.

Run scripts using existing repo tooling.
- If repo uses bun: bun run <script>
- Otherwise: npm run <script>

If ts execution is needed and tsx is missing:
- Add devDependency "tsx" and a script alias.
